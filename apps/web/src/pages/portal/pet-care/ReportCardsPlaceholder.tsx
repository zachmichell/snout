import PlaceholderPage from "../PlaceholderPage";
import { FileText } from "lucide-react";
export default function ReportCardsPlaceholder() {
  return <PlaceholderPage title="Report Cards" description="Create and publish report cards for pet stays. This feature is under development." icon={FileText} />;
}

{
  "name": "snout",
  "private": true,
  "version": "0.0.0",
  "description": "Snout monorepo — web app, iOS app, shared Supabase backend",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/web dev",
    "build": "bun run --cwd apps/web build",
    "lint": "bun run --cwd apps/web lint",
    "typecheck": "bun run --cwd apps/web typecheck",
    "test": "bun run --cwd apps/web test",
    "test:watch": "bun run --cwd apps/web test:watch",
    "test:integration": "bun run --cwd apps/web test:integration",
    "types:gen": "supabase gen types typescript --project-id empdnuzfjgfnphwauhah > packages/shared-types/src/database.ts"
  }
}
